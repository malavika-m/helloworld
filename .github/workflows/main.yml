name: Windows C/C++ CI

on:
  push:

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: gcc test.c -o hello.exe
    - name: Run
      run: .\hello.exe
